name: Prepare M2-T01

on:
  push:
    branches:
      - task/M2-T01-image-selection

permissions:
  contents: write

concurrency:
  group: prepare-m2-t01
  cancel-in-progress: false

jobs:
  prepare:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout task branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: task/M2-T01-image-selection
          fetch-depth: 0

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2
        with:
          flutter-version: 3.47.2
          channel: stable
          cache: true

      - name: Resolve and lock dependencies
        run: flutter pub get

      - name: Format task sources
        run: dart format lib test

      - name: Verify task sources
        run: |
          python3 tool/normalize_platforms.py --check
          flutter analyze --fatal-infos
          flutter test test/platform/image_picker test/features/print_job test/app

      - name: Commit lockfile and formatted sources
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add pubspec.lock lib test
          if git diff --cached --quiet; then
            echo 'Lockfile and sources were already prepared.'
          else
            git commit -m 'M2-T01: lock image picker and format selection flow'
            git restore --worktree .
            git pull --rebase origin task/M2-T01-image-selection
            git push origin HEAD:task/M2-T01-image-selection
          fi
