name: Prepare M1-T04

on:
  push:
    branches:
      - task/M1-T04-pdf-preview-share-spike

permissions:
  contents: write

concurrency:
  group: prepare-m1-t04
  cancel-in-progress: false

jobs:
  prepare:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout task branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: task/M1-T04-pdf-preview-share-spike
          fetch-depth: 0

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2
        with:
          flutter-version: 3.47.2
          channel: stable
          cache: true

      - name: Resolve and lock dependencies
        run: flutter pub get

      - name: Format task sources
        run: dart format lib test

      - name: Verify task sources
        run: |
          python3 tool/normalize_platforms.py --check
          flutter analyze --fatal-infos
          flutter test

      - name: Commit lockfile and formatted sources
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add pubspec.lock lib test
          if git diff --cached --quiet; then
            echo 'Lockfile and sources were already prepared.'
          else
            git commit -m 'M1-T04: lock printing dependency and format spike'
            git restore --worktree .
            git pull --rebase origin task/M1-T04-pdf-preview-share-spike
            git push origin HEAD:task/M1-T04-pdf-preview-share-spike
          fi
