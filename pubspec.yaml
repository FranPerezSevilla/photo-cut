name: photo_cut
description: Print one photo repeatedly at an exact physical size.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  pdf: 3.13.0
  printing: 5.15.0

dev_dependencies:
  flutter_lints: 6.0.0
  flutter_test:
    sdk: flutter
